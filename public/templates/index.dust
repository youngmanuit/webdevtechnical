{>"layouts/master" /}

{<body}
    {#books}
        <div class="large-3 columns book end">
            <img src="/img/{.cover}">
            <h4>{.title}</h4>
            <p>{.truncText}</p>
            <div class="price">Mua ngay với <span>${.price}</span></div>
            <br>
            <a href="/books/details/{._id}" class="button small">Chi tiết</a>
        </div>
    {/books}
{/body}

